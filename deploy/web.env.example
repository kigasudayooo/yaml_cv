# deploy/yaml-cv.container が読み込む環境変数ファイルのサンプル。
# 実運用では ~/.config/yaml_cv/web.env にコピーし、必ず固有の値に変更すること。
# このファイル自体（値を入れたもの）はコミットしない。

WEBAPP_BASIC_AUTH_USER=change-me
WEBAPP_BASIC_AUTH_PASSWORD=change-me-too
